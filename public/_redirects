
# Netlify redirects file
# These rules will change if you change your site's custom domains or HTTPS settings

# Redirect default Netlify subdomain to primary domain
https://*.netlify.app/* https://pixcraftai.com/:splat 301!

# Handle Single Page Application routing
/* /index.html 200

# Explicit redirects for common paths
/image-generator /index.html 200
/auth /index.html 200
/image-to-prompt /index.html 200
/image-upscaler /index.html 200
/graphic-designer-bot /index.html 200
/bulk-image-size-increaser /index.html 200
/metadata-generator /index.html 200

